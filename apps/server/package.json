{
  "name": "@openclaw/server",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run src/index.ts",
    "test": "bun test tests",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/zod-validator": "latest",
    "@openclaw/types": "file:../../packages/types",
    "@opentelemetry/api": "latest",
    "hono": "latest",
    "paseto": "latest",
    "pino": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
